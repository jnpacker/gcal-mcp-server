version: "2"

run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - misspell

issues:
  exclude-rules:
    # Allow fmt.Fprintf(os.Stderr, ...) — required pattern for MCP server logging
    - linters: [forbidigo]
      text: "fmt.Fprintf"
